"""Note-taking application — top-level package.

Sub-packages are imported explicitly by their consumers; this module
intentionally exposes nothing so that import order between layers is
visible at every call site.
"""
