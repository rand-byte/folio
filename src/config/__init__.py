"""Application-level configuration: paths, defaults, seed data.

Modules here are read at startup and contain only constants or pure
filesystem-resolution helpers. No I/O against the database, no GTK
imports.
"""
